import { params } from './params';
import { response } from './response';

export const unFavoriteAuctionPropertySchema = {
  tags: ['AuctionProperty'],
  description: 'Unfavorite auction property',
  params,
  response,
};
