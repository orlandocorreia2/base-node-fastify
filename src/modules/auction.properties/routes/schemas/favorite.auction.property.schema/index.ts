import { params } from './params';
import { response } from './response';

export const favoriteAuctionPropertySchema = {
  tags: ['AuctionProperty'],
  description: 'Favorite auction property',
  params,
  response,
};
