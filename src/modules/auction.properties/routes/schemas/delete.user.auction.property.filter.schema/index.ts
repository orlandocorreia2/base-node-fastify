import { params } from './params';
import { response } from './response';

export const deleteUserAuctionPropertyFilterSchema = {
  tags: ['AuctionProperty'],
  description: 'Delete user auction property filter',
  params,
  response,
};
